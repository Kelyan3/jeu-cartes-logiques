/**
 * Affiche le texte reçu avec le style typographique utilisé pour la notation logique.
 */
const LogicText = ({ children }) => {
	return <span className="math-text">{children}</span>;
};

export default LogicText;