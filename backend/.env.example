# Copiez ce fichier en .env et remplissez les valeurs (voir le README, section "Variables d'environnement", pour le détail).
DB_USER=cartes_logiques_user
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cartes_logiques

# Insérez ici la clé de votre choix (32 caractères recommandés).
SECRET_KEY=

# Uniquement en production (fournie automatiquement par certains hébergeurs) :
#DATABASE_URL=
#FRONTEND_URL=